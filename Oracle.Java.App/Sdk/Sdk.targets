<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  
  <Target Name="Build" >
    <Exec Command="javac -d $(MSBuildProjectDirectory)/$(OutputPath) @(Compile)" />
  </Target>

</Project>
